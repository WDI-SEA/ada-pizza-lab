<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
  </head>
  <body>
  	<h1>pizza-single.ejs</h1>
  	<% pizza.forEach(pie => { %>
  		<% if (pie.flavorKey === id || pie.locationKey === id){ %>
  		<div>
  			<h1><%= pie.flavor %></h1>
  			<h3><%= pie.location %></h3>
  			<img src="<%= pie.image %>">
  		</div>
  		<% } %>
  	<% }) %>
  </body>
</html>